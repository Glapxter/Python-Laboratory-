import random
import time

inicio = time.perf_counter()

def bubble_sort(lista):
    comparaciones = 0
    intercambios = 0
    for i in range(len(lista)):
        print(lista)
        huno_intercambio = False
        for j in range(len(lista) - 1 - i):
            comparaciones += 1
            if lista[j] > lista[j+1]:
                intercambios += 1
                lista[j], lista[j+1] = lista[j+1], lista[j]
                huno_intercambio = True
        if not huno_intercambio:
            break
    print("Comparaciones realizadas: ", comparaciones)
    print("Intercambios realizados: ", intercambios)
    return lista

fin = time.perf_counter()
tiempo = fin - inicio
datos = list(range(1000, 0, -1))
#list(range(1000))
#random.sample(range(1, 10000), 1000)
#[random.randint(1, 100) for _ in range(20)]
lista = datos.copy()

print("Lista original:")
print(datos)
print("Lista ordenada")
print(bubble_sort(lista))
print("Medición de tiempo: ", fin)